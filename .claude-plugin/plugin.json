{
  "name": "wallet",
  "description": "Agent wallet plugin for Claude Code — decide per-request via MCP tools (approve / refuse / reject_with_error). Publishes JWT-signed wallet.sign.response messages directed at the wallet-vault Wire integration.",
  "version": "0.2.0",
  "keywords": [
    "wallet",
    "ethereum",
    "eip1193",
    "agent",
    "mcp",
    "channel"
  ],
  "postInstall": "bun install --frozen-lockfile",
  "postUpdate": "bun install --frozen-lockfile"
}
