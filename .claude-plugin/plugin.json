{
  "name": "wallet",
  "description": "Agent wallet plugin for Claude Code — sign decisions (approve / refuse / reject_with_error) + vault management (wallet_list / wallet_use + operator-gated grant / revoke / set_access_mode). Edits wallet directory via Wire's plugin_settings.",
  "version": "0.4.0",
  "keywords": [
    "wallet",
    "ethereum",
    "eip1193",
    "agent",
    "mcp",
    "channel"
  ],
  "postInstall": "bun install --frozen-lockfile",
  "postUpdate": "bun install --frozen-lockfile"
}
